#include "main.h"
/**
 * _isdigit - checks if the given input is a number
 * Return: returns 1 if its a number 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
else
{
return (0);
}
}
